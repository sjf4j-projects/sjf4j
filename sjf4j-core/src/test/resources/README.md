# Test resources

Store resources under the package path of the tests that consume them. Keep short data
inline in the test; use this directory for larger fixtures or fixtures shared across
multiple formats.
